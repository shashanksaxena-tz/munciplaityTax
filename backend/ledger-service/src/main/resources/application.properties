spring.application.name=ledger-service
server.port=8087

# Eureka Client
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# PostgreSQL
spring.datasource.url=jdbc:postgresql://pg-1ac838cf-taazaa-6110.g.aivencloud.com:23581/defaultdb?ssl=require
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_CIZODtyC1VgMeY5KcC3
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
# Disable validation during development to avoid checksum mismatch errors
# In production, use 'flyway repair' to update checksums after migration file changes
spring.flyway.validate-on-migrate=false
spring.flyway.out-of-order=true
spring.flyway.table=flyway_schema_history_ledger

# Mock Payment Provider Settings
ledger.payment.mode=TEST
ledger.payment.provider=MOCK
